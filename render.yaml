services:
  - type: web
    name: wedding-site-redesign
    runtime: static
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: ./public
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
